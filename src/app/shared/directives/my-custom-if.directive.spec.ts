import { MyCustomIfDirective } from './my-custom-if.directive';

describe('MyCustomIfDirective', () => {
  it('should create an instance', () => {
    const directive = new MyCustomIfDirective();
    expect(directive).toBeTruthy();
  });
});
